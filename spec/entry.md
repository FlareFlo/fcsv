An entry is seperated by `;`.  
Strings are enclosed by either matching `'` or `"`.
Every entry should be terminated with a `;` including the last for each line.  
A `\n` LF signifies the end of an entry.

Example:  
`"stuff";"3";"1605959";`<br>